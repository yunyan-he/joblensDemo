# Environment — never commit real credentials
.env

# Input files — keep your JD and CV private
jd.txt
cv.txt

# Output
analysis_result.json

# RAG — personal documents (never commit)
knowledge_base/

# RAG — local vector store (auto-generated, never commit)
chroma_db/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
